{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The app talks to its own Node sidecar over localhost HTTP, not Tauri IPC, so the surface is intentionally minimal.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
