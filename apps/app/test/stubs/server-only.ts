// Test stub for the `server-only` package. The real module throws when imported
// outside a React Server Component bundle; under Vitest (plain Node) we alias it
// to this no-op so server-only modules can be unit-tested directly.
export {}
