# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# turbo
.turbo

# production
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env*.local

# local build on-disk state (PRD #404) — never committed:
#  - PGlite embedded-Postgres files at PGLITE_DATA_DIR (defaults to ./.pglite)
#  - the local Yjs host's persisted Y.Docs at YJS_PERSISTENCE_DIR
#    (defaults to ./.data/yjs)
.pglite/
*.pglite
data/
.data/

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
